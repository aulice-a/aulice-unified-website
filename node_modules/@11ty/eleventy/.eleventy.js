module.exports = function(eleventyConfig) {
    // Set the input directory to 'src' and the output directory to 'dist'
    return {
      dir: {
        input: "src", 
        output: "dist" 
      }
    };
  };